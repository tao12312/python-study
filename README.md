API / 모델 설정
최상단에서 Vertex 서비스 계정 JSON 입력
VERTEX_PROJECT_ID, VERTEX_LOCATION 설정 가능
문제 출제 모델과 피드백 모델 분리
PROBLEM_GEMINI_MODEL: 문제 출제 전용 모델
GEMINI_MODEL: 피드백/주관식 채점용 모델
문제 생성 응답 최대 출력 토큰 30000
API 요청 타임아웃 180초
429 Too Many Requests 발생 시 재시도 및 대기 로직 있음
AI 응답이 JSON 뒤에 추가 텍스트를 붙여도 첫 JSON만 추출해서 파싱
프롬프트 관리
시스템 프롬프트, JSON 출력 스키마, 모드별 프롬프트, 유형별 프롬프트, EX 모드 프롬프트, 피드백 프롬프트를 prompts.json에서 관리
코드 안에는 프롬프트 본문 대신 필요한 키 구조만 유지
문제 생성 시 모드와 유형에 맞는 프롬프트를 자동 조합
AI에게 JSON 형식 출력을 강제
잘못된 응답이나 요청 실패 시 재시도 화면 제공
개념서 관리
concepts 폴더의 .txt 파일을 개념서로 사용
개념서 추가 가능
외부 .txt 파일 가져오기 가능
새 개념서 직접 작성 가능
개념서 삭제 가능
개념서 수정 가능제목 수정
본문 수정
제목 변경 시 파일명도 변경
이미 같은 이름이 있으면 저장 차단
출제 범위로 선택된 파일명을 바꿔도 선택 상태 유지

학습/시험 출제 범위로 개념서 여러 개 선택 가능
선택한 범위는 다음 실행에도 유지
선택한 개념서가 없으면 전체 개념서를 범위로 사용
EX 모드
메인 화면에서 EX 모드 ON/OFF 가능
EX 모드는 파이썬 코드 실행 없이 일반 과목 문제를 풀 수 있는 모드
EX 모드 상태는 저장되어 다음 실행에도 유지
EX 모드에서는 주관식 답안을 .py 실행 대상으로 보지 않고 텍스트 답안으로 처리
EX 모드 전용 객관식/주관식 프롬프트와 피드백 프롬프트 사용
개념 모드
개념서 목록에서 원하는 단원 선택
개념 텍스트 읽기 화면 제공
개념 읽기 화면에 메인 메뉴로 돌아가기 버튼 있음
선택한 개념서 하나만 기반으로 객관식 5문제 생성
문제는 한 문제씩 출제
객관식 선택 후 즉시 정답/오답 표시
오답이면 해당 선지 피드백 표시
힌트 보기 가능
문제 텍스트가 길면 문제 칸 내부에서 스크롤 가능
학습 모드
객관식 학습 또는 주관식 학습 선택 가능
선택된 개념서 범위를 기반으로 문제 10개 생성
객관식은 한 문제씩 풀이하고 즉시 정답/오답 확인
주관식은 코드 또는 EX 모드 텍스트 답안을 작성해 채점
파이썬 모드 주관식은 로컬에서 실제 Python 실행
실행 성공/실패 결과를 AI 피드백에 함께 전달
무한 루프 방지를 위한 실행 타임아웃 있음
주관식 입력칸 위치와 여백 조정됨
문제 텍스트가 길면 문제 칸 내부 스크롤 가능
시험 모드
선택된 개념서 범위에서 총 10문제 생성
1~5번은 객관식 고정
6~10번은 주관식 고정
모든 문제가 한 페이지에 스크롤 형태로 표시
각 문항별 힌트 보기 가능
각 문항별 임시 저장 가능
최종 제출 시 임시 저장을 누르지 않은 답안도 자동 저장
진짜 빈 문항만 제출 전 확인
제출 후 채점 진행 상태 표시
채점 완료 후 각 문제 아래에 정답 여부와 피드백 표시
제출 후 빨강/초록 결과 표시 상태에서도 스크롤 동작 개선됨
문제 텍스트가 길면 문제 칸 내부에서 따로 스크롤 가능
객관식 채점
선지 5개 구조 사용
정답 번호 key 기반 채점
정답 선지는 초록색 표시
오답 선택지는 빨간색 표시
정답 선지도 함께 초록색 표시
오답 이유는 wrong_feedbacks에서 선택한 선지에 맞게 표시
주관식 채점
문제, 힌트, 평가 기준 사용
파이썬 모드에서는 사용자의 코드를 임시 파일로 저장 후 실행
실행 결과 stdout/stderr 수집
실행 결과와 사용자 답안을 AI에게 보내 최종 판정
EX 모드에서는 코드 실행 없이 텍스트 답안으로 AI 판정
기존 temp_exec.py 권한 문제를 피하도록 임시 파일 방식 개선됨
기록실
풀이 기록 저장
과거 기록 파일 확인 가능
기록실에서 저장된 파일 목록 표시
기록 파일 선택 후 문제, 내 답, 정답 여부, 피드백 확인 가능
기록 보기 화면 스크롤 문제 수정됨
과거 문제로 다시 풀기 가능
기록실 UI 오류였던 pack(width=...) 문제 수정됨
저장 구조
library 폴더에 풀이 기록 저장
모드명과 날짜/시간 기반 파일명 사용
문제 세트와 사용자 결과를 JSON 구조로 저장
기록에는 문제, 사용자 답안, 정답 여부, 피드백 포함
UI 개선
긴 문제 텍스트는 스크롤 가능한 문제 박스로 표시
시험지 전체 스크롤과 문제 칸 내부 스크롤 분리
주관식 답안 입력칸 여백 조정
AI 요청 실패 화면에 재시도 버튼과 메뉴 복귀 버튼 제공
시험 제출 시 미저장 답안 자동 저장 안내
개념서 관리 화면에서 추가/수정/삭제/범위 저장 지원
