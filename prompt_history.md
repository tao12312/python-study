# 🤖 프롬프트 엔지니어링 및 고도화 기록

본 프로젝트에서 AI 모델의 출제 정확도를 극대화하고, 웹/앱 연동 시 가장 빈번하게 발생하는 JSON 파싱 에러를 원천 차단하기 위해 진행한 프롬프트 고도화 기록입니다.

---

## 1. 초기 프롬프트 및 문제점 (Before)
* **초기 목적:** 파이썬 초보자를 위한 단계별 문제(객관식/주관식) 출제 및 코드 실행 결과 피드백.
* **발생한 결함 (Issue):**
  1. **JSON 파싱 에러:** AI가 응답을 줄 때 서론/결론 인사말을 붙이거나 ` ```json ` 같은 마크다운 코드 블록을 포함하여 프로그램이 멈추는 현상 발생.
  2. **출제 범위 이탈:** 제공된 개념 범위를 벗어난 뜬금없는 파이썬 문법이 출제되어 초보자 학습 흐름을 방해함.
  3. **모호한 채점 기준:** 사용자의 코드에 사소한 오타가 있어도 AI가 대충 `true`로 판정해 버리는 느슨한 피드백 문제 발생.

---

## 2. 프롬프트 고도화 및 반영 사항 (After)
이러한 결함들을 해결하기 위해 구조화된 JSON 데이터만 추출하도록 프롬프트 엔지니어링을 적용했습니다.

* **출력 구조의 엄격한 제어:** 인사말, 마크다운 코드 블록을 전면 금지하고 **"출력의 첫 글자는 반드시 '{', 마지막 글자는 반드시 '}'"**여야 함을 명시하여 파싱 에러율 0% 달성.
* **컨텍스트 제한 (환각 방지):** 범위 외의 지식 출제를 엄격히 제한하고, 텍스트 내에서만 유추할 수 있는 문항만 생성하도록 단속.
* **채점 및 피드백 루프 강화:** 표준 출력(stdout) 결과 및 평가 기준과 100% 만족하지 않으면 **무조건 `false`를 반환**하도록 엄격한 자동 채점 가이던스 수립.

---

## 3. 최종 반영된 config.json 구조 (주인님 완성본 원본)
```json
{
  "config": {
    "system_prompt": "당신은 파이썬 초보 학습자를 가르치는 전문 튜터이자, 오직 결함 없는 정교한 JSON 데이터만 생성하는 자동화 시스템입니다. 반드시 요구된 JSON 구조 외의 서론, 결론, 인사말, 설명, 마크다운 코드 블록(```json 등)을 절대 출력하지 마세요. 출력의 첫 글자는 반드시 '{'여야 하며, 마지막 글자는 반드시 '}'여야 합니다.",
    "mode_prompts": {
      "concept": "[출제 모드: 개념 집중 학습]\n제공된 단일 개념 텍스트의 범위 안에서만 완벽히 유추할 수 있는 객관식(objective) 문제 5개를 출제하세요. 개념 텍스트에 언급되지 않은 파이썬 지식은 절대로 출제 범위에 포함해서는 안 됩니다.",
      "learning": "[출제 모드: 유형별 선택 학습]\n제공된 전체 파이썬 개념 범위를 기반으로, 사용자가 선택한 유형({type_desc})에 부합하는 문제를 누락 없이 정확히 10개 생성하여 'quiz_set' 배열에 담아 반환하세요.",
      "test": "[출제 모드: 최종 모의고사]\n제공된 전체 파이썬 개념 범위에서 총 10개의 시험 문제를 균형 있게 출제하세요. 규칙인 '1번부터 5번까지는 무조건 객관식(objective)', '6번부터 10번까지는 무조건 주관식 코딩(subjective)' 문항으로 순서를 완벽히 고정하여 'quiz_set' 배열에 담아 반환해야 합니다."
    },
    "type_prompts": {
      "objective": "[문항 유형: 객관식(objective)]\n각 문항은 다음 요소를 완벽히 준수해야 합니다:\n- type 필드는 반드시 'objective'로 설정\n- options 배열에는 중복 없는 명확한 보기 5개를 포함\n- key 필드는 정답 보기가 위치한 인덱스 번호(1~5 범위의 정수)를 기입\n- wrong_feedbacks 배열에는 정답 보기를 제외한 나머지 4개의 오답 보기 순서에 맞춰, 사용자가 그것을 골랐을 때 왜 오답인지를 개별 설명하는 4개의 문자열을 인덱스 순서대로 포함\n- hint 필드에는 직접적인 정답 언급 없이 초보자가 힌트를 얻을 수 있는 개념적 가이드를 제공",
      "subjective": "[문항 유형: 주관식 코딩(subjective)]\n각 문항은 다음 요소를 완벽히 준수해야 합니다:\n- type 필드는 반드시 'subjective'로 설정\n- question 필드에는 초보자가 5줄에서 15줄 이내로 파이썬 코드를 작성하여 해결할 수 있는 명확한 문제 시나리오를 제시\n- evaluation_criteria 필드에는 사용자가 작성한 코드가 로컬에서 실행되었을 때, 표준 출력(stdout)으로 나와야 하는 정확한 문자열 결과나 필수 포함 키워드 등 정답을 판별할 수 있는 엄격한 텍스트 기준을 명시\n- hint 필드에는 사용할 함수나 조건문의 뼈대 등 구현 힌트를 제공"
    },
    "feedback_prompt": "[역할: 엄격한 채점 및 피드백기]\n제시된 [문제 내용], [평가 기준], [사용자가 제출한 파이썬 코드], 그리고 코드가 [로컬에서 실제 실행된 결과]를 종합적으로 비교 분석하세요.\n사용자의 코드가 문제의 의도와 평가 기준을 100% 만족하는지 엄격히 최종 판정하세요. 오타나 사소한 에러로 실행 결과가 일치하지 않으면 반드시 무조건 false 처리해야 합니다.\n\n출력은 오직 아래의 JSON 포맷 하나만 허용됩니다:\n{\n  \"is_correct\": true 또는 false (논리값),\n  \"feedback\": \"코드의 장단점을 분석하고, 틀렸다면 구체적인 수정 방향을 친절하게 제시하는 2~3문장의 완성된 설명문\"\n}"
  }
}