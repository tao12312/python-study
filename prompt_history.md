# 🤖 프롬프트 엔지니어링 및 고도화 기록

본 프로젝트에서 AI 모델의 출제 정확도를 극대화하고, 웹/앱 연동 시 가장 빈번하게 발생하는 JSON 파싱 에러를 원천 차단하기 위해 진행한 프롬프트 고도화 기록입니다. 조원들의 시스템 스키마 요구사항과 원본 한글 지시문을 100% 융합하여 작성했습니다.

---

## 1. 초기 프롬프트 및 문제점 (Before)
* **발생한 결함 (Issue):**
  1. **JSON 파싱 에러:** 프로그램 연동 테스트 중 AI 응답에 마크다운 코드 블록(```json) 및 인사말이 섞여 들어와 시스템이 다운되는 오류 발생.
  2. **출제 스코프 이탈:** 개념 학습 및 모의고사 모드에서 지정된 단일 개념 문서 외의 무관한 파이썬 문법과 개념이 출제되는 오작동 확인.
  3. **자동 채점 판정 불일치:** 사소한 텍스트 불일치나 문법 오류가 있음에도 AI가 주관식 답변 및 코드를 무분별하게 정답(true) 처리하는 결함 발견.

---

## 2. 프롬프트 고도화 및 반영 사항 (After)
새로운 시스템 연동 규격(`prompts.json`)에 맞춰 지시 프롬프트를 전면 리팩토링했습니다.

* **엄격한 출력 포맷 제어 (`system_prompt`, `schema_prompt` 고도화):**
  - "첫 글자는 반드시 '{', 마지막 글자는 반드시 '}'여야 함"을 이중 제어 장치로 이식하여 파싱 에러 완벽 해제.
  - 서론, 결론, 설명문 및 마크다운 코드 블록 표기 전면 봉쇄.
* **학습 범위 및 정렬 고정 (`mode_prompts` 고도화):**
  - 개념 텍스트에 언급되지 않은 지식의 출제를 차단하는 환각 방지 지시문 강화.
  - 시험 모드 시 1~5번 객관식, 6~10번 주관식 순서 고정 규칙 주입.
* **채점 정밀도 및 피드백 강화 (`feedback_prompt`, `ex_feedback_prompt` 고도화):**
  - 파이썬 코드 로컬 실행 결과(stdout) 매칭 및 엄격한 무조건 false 처리 규칙 마련.
  - 일반 과목 모드(`ex`) 채점 시 핵심 키워드 유무를 엄격하게 판단하는 기준 이식.

---

## 3. 최종 반영 규격 (조원 협업 및 프롬프트 연동)
* `system_prompt`, `schema_prompt`, `mode_notes`, `mode_prompts`, `type_prompts`, `ex_type_prompts`, `request_prompts`, `feedback_prompt`, `ex_feedback_prompt` 전체 키 구조 및 템플릿 변수와 100% 호환 보장.